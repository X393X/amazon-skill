{
  "mode": "private_internal",
  "note": "This repository does not store provider connection settings.",
  "provider_response_path": "runtime/provider_response.json",
  "rules": [
    "Connect your MCP or Sorftime-compatible provider outside this repository.",
    "Save the local provider response JSON in an ignored directory.",
    "Run scripts/run_from_provider_response.py against that local JSON.",
    "Do not commit provider responses or private generated reports."
  ]
}
